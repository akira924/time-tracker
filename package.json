{
  "name": "time-tracker",
  "version": "1.0.0",
  "description": "Posts a Slack check-in every 2 hours from 8 AM to 10 PM CDT.",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "start": "node index.js",
    "test:ping": "node index.js --ping-now"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "node-cron": "^3.0.3"
  }
}
